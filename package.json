{
  "name": "grono-map",
  "version": "1.2.0",
  "description": "",
  "author": "",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "migration:run:api": "turbo run migration:run --filter=api"
    
  },
  "devDependencies": {
    "prettier": "3.5.3",
    "husky": "9.1.7",
    "turbo": "^2.5.5"
  },
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
